export default function App() {
  return (
    <main className="shell">
      <section className="placeholder" aria-labelledby="app-title">
        <p className="eyeline">LoopLedger</p>
        <h1 id="app-title">Agent QA evidence ledger</h1>
        <p>
          Track requirements, TestSprite CLI runs, failures, fixes, reruns, cost notes, and export
          a credible LOOP.md.
        </p>
      </section>
    </main>
  );
}
